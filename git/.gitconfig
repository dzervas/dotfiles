[alias]
	aa = !git add -A && git status
	ac = !git aa && git commit
	acp = !git ac && git push
	c = clone
	co = checkout
	d = diff
	#graph = 'log —graph —pretty="\'"%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset"\'" —abbrev-commit —all'
	#g = graph
	# Parse positional params
	hub = !"f() { git clone github.com:$1; }; f"
	s = status
[color]
	ui = auto
[commit]
	gpgsign = true
[core]
	autocrlf = input
	excludesfile = ~/.gitignore
[push]
	default = simple
[user]
	name = Dimitris Zervas
	email = dzervas@dzervas.gr
	signingkey = 528636D3
[web]
	browser = chromium
