font:
  size: 12
  normal:
    family: Iosevka Term

# hints:
  # enabled:
    # # Matches:
    # # src/main.rs:17:1
    # # src/main.rs:17
    # # File "/home/dzervas/Lab/pcb_mill_calc.py", line 12, in <module>
    # - regex: |-
        # ["']?(/?(?:\w+/)+)?(\w+\.\w+)(?:["'], line |:)(\d+)(:\d+)?
      # # '
      # # hyperlinks: true
      # post_processing: true
      # command:
        # program: notify-send
        # args:
          # # - --reuse-window
          # # - --goto
          # # <path>/<filename>:<linenum>:<column>
          # # path includes the trainling '/' and column includes the leading ':'
          # - --app-name
          # - '$1'
      # mouse:
        # enabled: true
        # mods: None

    # - regex: |-
        # [\s'"]([\w/]+)[\s'"]
      # post_processing: true
      # command:
        # program: xdg-open
        # args:
          # - '{1}'
      # mouse:
        # enabled: true
        # mods: Control
